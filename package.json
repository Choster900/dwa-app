{
  "scripts": {
    "backend": "json-server --watch json/db.json",    
    "server": "httpserver 81 localhost"    
  },
  "dependencies": {
    "httpserver": "^0.3.0",
    "json-server": "^1.0.0-beta.3"
  }
}
