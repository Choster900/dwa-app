{
  "scripts": {
    "backend": "json-server --watch json/db.json",    
    "server": "nodemon --exec httpserver 99 localhost"
  },
  "dependencies": {
    "httpserver": "^0.3.0",
    "json-server": "^1.0.0-beta.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
